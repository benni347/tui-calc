pub fn square(side_length: f64) -> f64 {
    // This function calculates the area of a square
    side_length.powi(2)
}

pub fn square_perimeter(side_length: f64) -> f64 {
    // This function calculates the perimeter of a square
    4.0 * side_length
}
